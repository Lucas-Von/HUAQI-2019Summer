<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="financial_management.data.product.FundMapper">
    <select id="selectSelfFund" resultType="financial_management.entity.MyFundPO">
        select user_id userId,
         balance balance,
         update_time updateTime,
         pay_password payPassword
         from my_fund
    </select>

    <select id="selectFund" resultType="financial_management.entity.FundPO">
        select name name, rate rate, `time` time from fund
    </select>

    <update id="updateSelfFund" parameterType="financial_management.entity.MyFundPO">
        update my_fund set balance = #{balance},update_time = #{update_time} where user_id = #{userId}    </update>

    <update id="updateSelfPassword">
        update my_fund set pay_password = #{payPassword} where user_id = #{userId}
    </update>

    <insert id="insertNewFund">
        insert into my_fund (user_id, balance) values (#{userId},#{balance})
    </insert>
</mapper>